package com.krmu.nexus.viewmodel
import androidx.lifecycle.ViewModel
open class BaseViewModel : ViewModel() {
    // This is the Parent of All ViewModels and here, we initialise the very Common
    // Logics like loading state, error handling etc
}